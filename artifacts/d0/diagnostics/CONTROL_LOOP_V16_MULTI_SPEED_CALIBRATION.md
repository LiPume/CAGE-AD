# CARLA Lincoln 多起始速度纵向标定 v16

状态：**通过；允许设计覆盖 0–2.0 m/s 的二维候选**

分支：`codex/apollo-d0-control-loop-repair-v16`

基线提交：`a929d71da6e2a40b1ceb76a4e5f1b5f473a9509a`

## 为什么需要多速度测量

V15 的候选只覆盖到 1.2 m/s，速度跟踪从约 0.54 提升到 0.647，但车辆超过覆盖范围后重新落回原表，仍未达到 0.70。禁止根据这次正式结果直接扩大同一候选；必须先测量速度维度。

## 冻结方法

仍然只启动 CARLA。Town01、Lincoln MKZ 2017、零其他车辆、20 Hz、自动变速箱和原车辆物理保持不变。

每个样本创建新车，用固定 `0.50` 油门预热到目标速度，确认实际为前进挡后，立即切换到待测油门。目标起始速度为 `1.0、1.5、2.0 m/s`；待测油门为 `0.30、0.35、0.40、0.45、0.50`；每个组合重复 3 次，共 45 个样本。每次记录 2 秒，前 0.25 秒过渡，后 1.75 秒用 V14 已冻结的普通最小二乘速度斜率计算净加速度。

## 通过条件

- 45 个样本全部在 12 秒内达到目标，起始速度位于目标到目标加 0.15 m/s；
- 40 帧完整，自动挡、真实前进挡、零刹车/倒车/手刹，控制回读误差小于 `1e-6`；
- 每个组合 3 次斜率极差不超过 `0.20 m/s²`；
- 每个起始速度内，随油门增加，斜率不得下降超过 `0.05 m/s²`；
- 每个起始速度都必须覆盖不大于 `0.10` 和不小于 `0.35 m/s²` 的响应。

如果失败，保留结果并停止扩大标定表。若通过，才允许设计覆盖 0–2.0 m/s 的二维候选；仍不代表通用车辆标定，不生成 D0 数据集或正式 TTC。

预算：10 分钟开机时间、256 MiB 新增文件。

## 测量结果

唯一一次运行 `CARLA_V16_MULTI_SPEED_01` 完成 45 个样本并通过全部冻结条件。所有组合的 3 次速度斜率极差都为 0。正式斜率矩阵如下，行是起始速度，列是 CARLA 油门：

| 起始速度 | 0.30 | 0.35 | 0.40 | 0.45 | 0.50 |
|---|---:|---:|---:|---:|---:|
| 1.0 m/s | -0.0134 | 0.0523 | 0.2905 | 0.5974 | 1.0585 |
| 1.5 m/s | -0.1938 | 0.0140 | 0.2220 | 0.5845 | 1.1288 |
| 2.0 m/s | -0.3478 | -0.1463 | 0.1643 | 0.6584 | 1.2317 |

单位均为 `m/s²`。45 个样本都从真实 1 挡开始；三行实际起始速度分别为 `1.0210、1.5234、2.0221 m/s`，均在冻结容差内。控制回读、自动挡、帧数和响应覆盖全部通过。

结果证明速度维度不能省略：例如相同 0.35 油门在 1.0 m/s 时轻微加速，在 2.0 m/s 时明显减速。V15 用一行低速关系覆盖到 1.2 m/s 后再回原表，无法维持更高速度，与这一测量一致。

本轮开机 54.14 秒，新增文件 732,698 字节。校验和：

- `summary.json`: `2737c31410bb79ce04519ae75de4771d585f9d1a3313cfa7d9b0dae46637ece1`
- `trace.jsonl`: `69041510df32509b2b22a1d9b5fe22209ae5ffd09276d584737cb7194657ebe6`
- `result.json`: `013409dbf5b3773cccc6d01bbabcf9c2d8ee0423cbc2c331390e143ccc4429ba`

## 最终判断

**V16 通过。** 允许使用这三行实测关系设计覆盖 0–2.0 m/s 的二维正加速度候选。

候选仍须逐速度线性反算，油门上限保持 0.50；负加速度和高于 2.0 m/s 的原表项保持不变。候选只做一次无车闭环验证，原 0.70 跟踪门槛不变。即使通过，也仍需在交互场景前做刹车和场景有效性 smoke，不能直接宣称数据集可用。
