# Apollo 0–2.0 m/s 二维候选标定表 v17

状态：**修复前合同已冻结，尚未运行**

分支：`codex/apollo-d0-control-loop-repair-v17`

基线提交：`5907a53c5c35fcf6b98c45159e840b5bd196274c`

前置车辆物理基线：V16-A 的唯一正式运行已通过。原始 Lincoln 在 10 秒满油门下达到 33.1505 m/s、升到 6 挡、无碰撞、200 帧控制回读完全一致，运行前后物理未变。因此 V17 不修改 CARLA 物理。V16-A 同时发现约 1.2 秒的启动换挡迟滞和随后加速度尖峰；该限制如实保留，V17 的既有门槛不因看到结果而修改。

## 候选生成规则

使用 V16 在 1.0、1.5、2.0 m/s 三个起始速度下的五档油门响应。每个速度内按实测净加速度反算 Apollo 命令；速度行之间线性插值；低于 1.0 m/s 使用 1.0 行，不向未知方向外推；超过实测最大正加速度时油门封顶 0.50，即 Apollo 命令 33.333%。

只替换原表中速度不高于 2.0 m/s且期望加速度不小于 0 的点。所有负加速度项和速度高于 2.0 m/s 的项逐项保留。候选仍通过运行专用 control flag/DAG 加载，不改 Apollo 安装目录。

## 冻结验证门槛

唯一一次 20 秒无车闭环必须满足：候选内部查表激活；400 帧完整；规划覆盖至少 95%；速度优化器失败 0；真实挡位反馈无假前进；横向偏离不超过 0.20 米；CARLA 油门不超过 0.50；刹车活跃不超过 5%；Apollo 滤波纵向加速度在 `[-6,2]`；速度跟踪比例仍须不低于 0.70。

若失败，本候选拒绝，不再查看结果后调第三版表。若通过，也只进入交互场景 smoke，不能直接生成正式数据集。

预算：10 分钟开机时间、256 MiB 新增文件。

## 测试结果

等待运行后填写。

## 最终判断

等待运行后填写。
