# Apollo—bridge—CARLA 同时刻挡位核验 v11

状态：**通过；真实挡位反馈已由同时刻证据确认**

分支：`codex/apollo-d0-control-loop-repair-v11`

基线提交：`a5f673708018c556514fd863603d4f65804e3f79`

## 为什么还要核验一次

V10 已消除“CARLA 实际为 0 挡、bridge 却回报前进挡”的假象，也没有让闭环退化。但是外部采集器把不同线程的最新消息放到同一行，在自动变速箱从 0 挡切到 1 挡时，出现 1 帧“上一条 Apollo 空挡消息 + 稍后的 CARLA 1 挡状态”。

这轮不再用异步拼接结果裁决修复。bridge 在构造底盘消息的同一段代码里，读取一次 CARLA 状态，并把“读取到的真实挡位”和“本次实际发布的 Apollo 挡位”成对写入同一条记录。

## 唯一允许的修改

- 给已有诊断日志增加记录类型；
- 每次发布 Apollo 底盘消息时，成对记录 CARLA 实际挡位、自动/手动挡状态和已发布挡位；
- 增加离线汇总工具和 CPU 测试。

不允许修改控制命令、挡位映射、油门、刹车、转向、加速度滤波、车辆物理、场景和数据协议。

## 通过条件

1. CPU 测试确认日志中的两侧值来自同一次 `send_vehicle_msgs` 调用；
2. 唯一一次 20 秒运行中，每条成对记录都符合 V10 冻结映射，错误数必须为 0；
3. 必须同时观察到真实 0 挡和 1 挡，自动挡始终开启；
4. 400 帧完整，无跳帧；规划覆盖率至少 95%，速度优化器失败 0 次，横向偏离不超过 0.20 米，前进至少 5.714 米；
5. 外部逐帧拼接仍单独报告，但不再用它代替同时刻证据。

本轮预算 10 分钟开机时间、256 MiB 新增文件。仍是控制闭环诊断，不是数据集，不运行正式 TTC。

## 测试结果

CPU 挡位与成对日志检查通过，仓库离线测试为 `165 passed`。

唯一一次闭环运行 `NO_NPC_V11_PAIRED_GEAR_01` 完整采集 400 帧、19.95 秒。bridge 在发布底盘消息的同一次调用中留下 900 条成对记录：

- CARLA 实际 0 挡：354 条；实际 1 挡：546 条；
- Apollo 发布挡位与冻结映射不一致：0 条；
- CARLA 实际不是 1 挡但 Apollo 发布前进挡：0 条；
- 手动换挡开启：0 条；
- 同时还记录 3134 条控制应用记录，没有更改控制行为。

外部 20 Hz 采集仍在换挡边界出现 1 行“CARLA 已到 1 挡、Apollo 最新消息还是空挡”。同一次 bridge 调用的成对证据为 0 错误，证明这 1 行来自两个异步数据源的采样先后，而不是挡位映射错误。

基础闭环结果：

- 400 帧完整、无跳帧；
- 规划有效覆盖率 98.25%；
- 速度优化器失败 0 次；
- 前进 15.017 米；
- 最大横向偏离约 0.020 米；
- CARLA 在 2.10 秒首次进入 1 挡；
- 最佳窗口速度跟踪比例约 0.540，仍低于 0.70。

旧通用总评仍为 `FAIL`：旧挡位指标把“控制要求前进、车辆自动变速箱暂时处于 0 挡”当成 42 帧错误；速度跟踪也仍未达标。这不推翻同时刻挡位核验，但说明车辆慢的问题尚未解决。

本轮开机 75.79 秒，新增文件 3,338,052 字节，未接近预算上限。关键校验和：

- `runtime_summary.json`: `45c8f0c585c8b028db9f9b7d24c957ef52d3f261fd1f4561a1d66c7fe6b08a53`
- `trace.jsonl`: `a2e9fe4f62605bb1bef335387872eb54537462f2f0f6f81cb954a4bbcb164cfa`
- `bridge_control_telemetry.jsonl`: `754f5fcefe554a1c0b6bb07d451a04987db2a5c8b3024b3f45482e104b1a11b0`
- `paired_gear_summary.json`: `f2f7b0c0d47f08b0efd4d054bff6a01a5c836f10393c1219d42a690312a726fe`
- `result.json`: `d9219275d5d89cf2a3e6eb8e39f13b5400317876e00ce4c95d6d5778d9f5e548`

## 最终判断

**V11 通过，V10 的真实挡位反馈修复可以确认保留。** 900 条同时刻证据全部一致，且基础行驶没有退化。

这只修好了“反馈说真话”，没有修好“车辆为什么只能达到计划速度约一半”。数据集和正式 TTC 继续暂停。下一步回到纵向闭环，先用已有 V11 曲线和 Apollo 标定表做离线对照，确定慢车来自 Apollo 输出偏小、bridge 比例换算，还是 CARLA Lincoln 对同一油门响应不足；在写下一份修复前合同前不再开仿真。
